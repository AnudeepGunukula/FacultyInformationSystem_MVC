

@{ ViewBag.Title = "Login";
    Layout = "~/Views/Shared/_Layout.cshtml"; }


<div style="padding-bottom:10%; padding-top:10%" class="col-4 offset-4">
    <div class="card">
        <div class="card-header">Login as</div>
        <div class="card-body">
            <div class="btn btn-block btn-danger" data-toggle="tooltip" data-placement="right" title="Logged as Admin">
                <i class="fas fa-user"></i>
                @*@Html.ActionLink("Admin", "Login", "Admin", null, new { style = "text-decoration:none", @class = "text-light" })*@
                <a class="nav-link text-light" asp-area="Identity" asp-page="/Account/Login">Admin</a>
            </div>
            <div class="btn btn-block btn-success" data-toggle="tooltip" data-placement="right" title="Logged as Faculty">
                <i class="fas fa-user-cog"></i>
                @*@Html.ActionLink("Faculty", "Login", "Faculty", null, new { style = "text-decoration:none", @class = "text-light" })*@
                <a class="nav-link text-light" asp-area="Identity" asp-page="/Account/Login">Faculty</a>
            </div>
            <div class="btn btn-block btn-primary" data-toggle="tooltip" data-placement="right" title="Logged as Student">
                <i class="fas fa-user-cog"></i>
                @*@Html.ActionLink("Student", "Login", "Student", null, new { style = "text-decoration:none", @class = "text-light" })*@
                <a class="nav-link text-light" asp-area="Identity" asp-page="/Account/Login">Student</a>
            </div>
        </div>
    </div>

</div>
